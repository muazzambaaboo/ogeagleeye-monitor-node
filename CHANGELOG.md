# Changelog

## 0.2.1

- Document install via the published npm package (`npm install @ogeagleeye/monitor-node`).
- Add package homepage and repository metadata for npm.

## 0.2.0

- Initial standalone repository split from the OGEagleEye monorepo.
- Node SDK (`@ogeagleeye/monitor-node`) with tsup + vitest.
